import { Routes } from '@angular/router';
import { AddExpensesComponent } from './expenses/addExpenses/addExpenses.component';

export const routes: Routes = [
    {path:"",component:AddExpensesComponent}
];
